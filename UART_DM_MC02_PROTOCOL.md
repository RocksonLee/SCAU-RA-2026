# RA8P1 到 DM_MC02 坐标串口协议

## 输出模式开关

开关位于 `vision_CPU1/src/uart_coordinate_protocol.h`：

```c
#define DM_COORDINATE_UART_ENABLE (0)
```

- `0`：默认模式。CPU1 将坐标转换为机械臂关节角度，并通过原有 CAN 接口控制电机。
- `1`：串口模式。CPU1 不发送上述 CAN 电机动作，改由 SCI0 将标定后的机械臂坐标发送给 DM_MC02。

## 串口参数

- 波特率：921600 bit/s
- 数据位：8
- 校验位：None
- 停止位：1
- 流控：无
- 字节序：多字节整数均为小端序

## 坐标帧

每个目标发送一个固定 24 字节的数据帧：

| 偏移 | 长度 | 字段 | 格式 |
|---:|---:|---|---|
| 0 | 1 | 帧头 0 | 固定 `0xAA` |
| 1 | 1 | 帧头 1 | 固定 `0x55` |
| 2 | 1 | 协议版本 | 当前为 `0x01` |
| 3 | 1 | 消息类型 | 坐标帧为 `0x01` |
| 4 | 4 | `pair_id` | `uint32_t`，小端序；同一批识别结果使用同一 ID |
| 8 | 1 | `class_id` | `0`=番茄，`1`=青葡萄，`2`=紫葡萄 |
| 9 | 1 | `flags` | 当前固定为 `0x00`，保留 |
| 10 | 4 | X | `int32_t`，小端序，单位 0.01 mm |
| 14 | 4 | Y | `int32_t`，小端序，单位 0.01 mm |
| 18 | 4 | Z | `int32_t`，小端序，单位 0.01 mm |
| 22 | 2 | CRC16 | CRC-16/CCITT-FALSE，小端序 |

坐标还原公式：

```text
coordinate_mm = signed_int32 / 100.0
```

CRC 参数：

```text
多项式  0x1021
初始值  0xFFFF
RefIn    false
RefOut   false
XorOut   0x0000
校验范围 字节 0～21（包含帧头，不包含 CRC 字段）
CRC 字节 低字节先发送
```

## 示例

以下目标：

```text
pair_id = 1
class_id = 0
X = 123.45 mm
Y = -67.89 mm
Z = 280.00 mm
```

发送帧为：

```text
AA 55 01 01 01 00 00 00 00 00 39 30 00 00 7B E5 FF FF 60 6D 00 00 81 C1
```

接收端应先查找连续帧头 `AA 55`，收满 24 字节后校验版本、消息类型和 CRC；校验失败时丢弃当前帧并重新搜索帧头。
